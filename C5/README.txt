At the moment, the project supports all requirements outlined in Milestone 3 - Final

To build this project, simply run main.sh

Note - This requires that the database exist in your local system.
Running this will install all dependencies (requests), and run the python script which will initialize the database.
